import * as React from "react";
import * as ReactDOM from "react-dom";
import ProfileIcon from "./ProfileIcon";

it("renders without crashing", () => {
  ReactDOM.render(<ProfileIcon />, document.createElement("div"));
});
